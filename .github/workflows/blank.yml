name: Test VKS
on: [push]

jobs:
  build:
    name: Create
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: echo hello world 
      - name: hello world1
        run: echo hello world1 
